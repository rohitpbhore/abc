<% if !@user.first.articles.blank? %>
  <ul>
    <% @user.first.articles.each do |a| %>
      <li id="article_<%= a.id %>">
        <%= a.text %>
        <%= link_to 'Destroy', [@user.first, a], method: :delete, remote: true, data: { confirm: 'Are you sure?' } %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>No Articles...</h3>
<% end %>
